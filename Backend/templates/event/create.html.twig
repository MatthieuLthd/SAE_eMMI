{# templates/event/create.html.twig #}

{% extends 'base.html.twig' %}

{% block body %}
    <h1>Créer un Événement</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ 'Créer'|trans }}</button>
    {{ form_end(form) }}
{% endblock %}
